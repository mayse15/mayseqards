# ── PRIVATE — never publish ──
# master spreadsheet (cost + value history)
Card_Collection.xlsx

# ── Internal project files (kept local, not part of the public site) ──
HANDOFF.md
BUSINESS-PLAN.md
CHEATSHEET.md
SHIPPING.md
MayseQards_BuildBrief.md
EBAY-AUTOMATION-BRIEF.md
.claude/
# local dev server
serve.py
# owner-only eBay sold-search tool
comp-lookup.html
# naming data bank (used by Claude, not the site)
checklists.json
# private value comps (sources/ranges/estimate basis) — only the derived tiers.js is public
values.js
gen_tiers.py
# private owner dashboard (value + P&L) — never deployed
dashboard.html
gen_dashboard.py
# owner-only Command Center portal (embeds the private dashboard + comp tool) — never deployed
home.html

# ── Legacy / superseded drafts ──
mayseqards.html
mayseqards-2.html
mayseqards-3.html
cards.json

# ── Large installers & OS cruft ──
*.dmg
.DS_Store

# The PUBLIC site (what deploys) is: index.html, storefront.html,
# collection.js, tiers.js, reviews.js, photos/  — plus .gitignore and DEPLOY.md.

# owner-only sourcing deal-scanner (never publish)
deals-watchlist.md
deals-report-*.md
deals-seen.json

# owner-only market-intel reports (never publish)
intel-report-*.md

# owner-only agents dashboard status feed (never publish)
agents-status.json

# internal agent tooling docs (never publish)
AGENT-DATA-EXTRACTION.md

# private master inventory record (has valuations) + its generator (never publish)
inventory.csv
inventory.md
gen_inventory.py

# bulk photo-upload tracker (which drive photos are processed) — never publish
photo-intake-log.json

# full-res card originals live on the external drive — never in the repo
photos-originals/
*-orig.jpg
*-original.*
youpick-map.json
youpick_description.txt
pack-pools.json
pack_rookie_desc.txt
pack_insert_desc.txt
price-overrides.json
build-list.json
build-done.txt
.build-done.txt
print_label.py
*_4x6.pdf

catalysts.js

selldesk.json
reinvest.json
gradeflip.json
wallet.json

# ── 24/7 scanner + eBay store manager (SECRETS — never publish) ──
# scanner/.cl-profile = live Card Ladder login session; ebay-manager/config.json + .tokens.json = eBay
# API keyset + OAuth refresh token. These must NEVER hit the public GitHub Pages repo.
scanner/
ebay-manager/
